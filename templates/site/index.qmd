---
title: "Job applications"
listing:
  id: applications
  contents:
    - private/applications/*/index.qmd
  type: table
  fields:
    - company
    - position
    - status
    - fit_score
    - date_found
  sort:
    - "fit_score desc"
    - "date_found desc"
  sort-ui:
    - company
    - position
    - status
    - fit_score
    - date_found
  filter-ui:
    - company
    - position
    - status
  date-format: "YYYY-MM-DD"
  feed: false
  table-striped: true
  table-hover: true
  page-size: 50
---

::: {.callout-note appearance="minimal"}
This page aggregates every application directory under `private/applications/`
that has been assembled by jobsmith. Sortable / filterable; each row links to
the per-app `index.html` (rendered separately — each app is its own Quarto
project so its include paths resolve).

Privacy: this site renders to `_site/` and should stay local — application
pages contain salary ranges, fit scores, and hiring-manager intelligence.
Run `jobsmith site render --public` to produce a sanitized variant in
`_site-public/` for sharing.
:::

::: {#applications}
:::
