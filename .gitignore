# macOS
.DS_Store
._*

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.uv-cache/
*.egg-info/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Node (in case templates/tools end up needing it)
node_modules/
package-lock.json

# Build artifacts
*.pdf
*.typ
_freeze/
.quarto/

# Per-user state (should NEVER be committed to the framework)
.apply-state/
applications/
private/
*.local.yml
*.local.yaml
config.yaml

# Logs and traces
*.log
# htmlgraph per-user state — generally untracked, with explicit exceptions for load-bearing plans
.htmlgraph/*
!.htmlgraph/plans/
.htmlgraph/plans/*
!.htmlgraph/plans/plan-807d44a7.yaml
!.htmlgraph/plans/plan-807d44a7.html

# OS / editor
*.bak
*.tmp
*.png
*.jpg
*.jpeg

# jobsmith framework repo — consumer files that belong in the user's repo
.apply-config.yaml
assets/

# Design sketches / WIP not yet integrated
src/jobsmith/llm/protocol.py

# Claude Code session data
.claude/

# Symlink artifacts from Quarto extensions
templates/extensions/_extensions/_extensions

# jobsmith
.apply-state/
private/applications/*/documents/*.pdf
private/applications/*/documents/*.typ
private/job_search.db
# Pipeline persistence (specialist outputs) — slice 1.
private/jobsmith.db
private/jobsmith.db-*
# Per-slug review state (amendments + chat history) — slice 1.
# Personal review notes; never check in.
private/.review/
private/.review-backups/
private/benchmarks/
private/feedback/
