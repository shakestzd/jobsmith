{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the Jobsmith desktop shell: core defaults plus permission to spawn ONLY the bundled Python sidecar (no arbitrary shell access).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "binaries/jobsmith-sidecar",
          "sidecar": true
        }
      ]
    }
  ]
}
