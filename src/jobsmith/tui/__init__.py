"""jobsmith TUI — interactive Textual surfaces.

Empty in 0.1. Populated in 0.2 with:
    - jobsmith dashboard      — live applications view
    - jobsmith calibrate      — interactive similarity-pair labeling
    - jobsmith review <slug>  — split-pane resume + cover letter review

To opt in early: `uv pip install -e ".[tui]"`
"""

from __future__ import annotations
