"""Email alert ingestion adapters for jobsmith (feat-b1bd050e).

Sub-modules:
  parsers  — template-based per-sender HTML parsers (LinkedIn, Indeed, Glassdoor)
  gmail    — Gmail API adapter (google-api-python-client)
  mailapp  — Mail.app CLI adapter (mail-app external tool)
"""
