---
description: Run the jobsmith apply pipeline for a JD URL
---

Run `jobsmith apply $ARGUMENTS` and stream the output. The pipeline phases are managed by the Python CLI; this slash command exists as a convenience wrapper for users who prefer slash-command invocation over the CLI.
